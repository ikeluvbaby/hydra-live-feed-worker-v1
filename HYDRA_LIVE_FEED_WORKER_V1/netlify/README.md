# Netlify Functions Folder

This file exists so GitHub preserves the `netlify/` folder separately from `functions/`.

Correct structure:

```text
HYDRA_LIVE_FEED_WORKER_V1/
  netlify/
    README.md
    functions/
      hydra-live-feed-worker.js
```
